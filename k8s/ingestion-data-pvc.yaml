apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ingestion-data
  namespace: rag-blueprint
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 2Gi  # 600MB data + headroom

